def maxStocks(stocks, amount):
    # output is supposed to be [10, [1, 3]]

    bestCombo = []
    # 1 to access the weight of the stock, n + 1 to access the next weight
    for s in range(1, n + 1):
        1 + (n + 1)
        if 1 + (n + 1) != amount:
            continue
        if 1 + (n + 1) == amount:
            bestCombo.append(1, n + 1)
        
    return amount, bestCombo

def main():
    stocks = [[1, 2], [3, 3], [5, 6], [6, 7]]
    amount = 10

if __name__ == "__main__":
    main()


